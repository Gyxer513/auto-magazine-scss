export class BurgerMenu {
  constructor(navBlock, burerButton, closeButton, logo) {
    this.navBlock = document.querySelector('nav')
    this.burerButton = burerButton;
    this.closeButton = closeButton;
    this.logo = logo;
    this.logoVisibleClass = logoVisibleClass
  }
_
}
